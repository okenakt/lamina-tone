export const TONESHEET_SIZE = {
  width: 200,
  height: 200,
} as const;

export const GRID_CONTROLS = {
  spacing: 40, // ToneSheet horizontal spacing
} as const;

export const AXIS_LIMITS = {
  minSize: 1,
  maxSize: 9,
} as const;
